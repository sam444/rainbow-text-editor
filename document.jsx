/**
 * @module Text Editor 
 */

export default class TextEditor{
    /** 
    * <div class="exapmle" data-module="text-editor" data-example='[{"common":"370-840"}]'></div>
    * @property {string}  id		                 -	Uniquely identifies an element.
    * @property {string}  value                  - Defining the value of an element.
    * @property {object}  dataSource             -Defining the elements of the data source.
    * @property {boolean} checkable                - 	Defines whether an element is displayed check box, the default is false.
    * @property {boolean}  searchable              - Defines whether an element is displayed search box, the default is false.
    * @property {boolean}  editable              - Defines whether the element can be edited, the default is false.
    * @property {boolean}  removeable              - 	Defines whether the element can be renamed, editable set to true when the take effect, the default is true.
    * @property {boolean}  renameable              - Defines a set of elements for the validation specified, between groups can be separated by a comma	Defines whether the element can be renamed, editable set to true when the take effect, the default is true.
    * @property {boolean}  moveable              -	Defines whether an element can drag, editable set to true when the take effect, the default is true.
    * @property {func}  onClick              -		Defining the element's click event.
    */
    example(){
        
    }
};



